apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: credentiald
  namespace: giantswarm
  labels:
    app: credentiald
spec:
  replicas: {{ .Values.deployment.replicas }}
  template:
    metadata:
      labels:
        app: credentiald
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - credentiald
              topologyKey: kubernetes.io/hostname
            weight: 100
      volumes:
      - name: credentiald-configmap
        configMap:
          name: credentiald-configmap
          items:
          - key: config.yml
            path: config.yml
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                  - key: k8s-app
                    operator: In
                    values:
                    - credentiald
              topologyKey: kubernetes.io/hostname
      serviceAccountName: credentiald
      securityContext:
        runAsUser: {{ .Values.pod.user.id }}
        runAsGroup: {{ .Values.pod.group.id }}
      containers:
      - name: credentiald
        image: {{ .Values.Installation.V1.Registry.Domain }}/giantswarm/credentiald:[[ .SHA ]]
        args:
        - daemon
        - --config.dirs=/var/run/credentiald/configmap/
        - --config.files=config
        volumeMounts:
        - name: credentiald-configmap
          mountPath: /var/run/credentiald/configmap/
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          timeoutSeconds: 1
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 5
          timeoutSeconds: 1
        resources:
          requests:
            cpu: 50m
            memory: 50Mi
          limits:
            cpu: 50m
            memory: 50Mi
      imagePullSecrets:
      - name: credentiald-pull-secret
